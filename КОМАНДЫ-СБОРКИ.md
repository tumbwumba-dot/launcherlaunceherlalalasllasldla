# Команды для сборки Reduxion Launcher

## ⚡ САМЫЕ ВАЖНЫЕ КОМАНДЫ

### 🔨 Пересобрать лаунчер
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && npx electron-builder --config electron-builder-launcher.yml --win
```
📁 **Результат:** `launcher-dist\ReduxionLauncher.exe`

### 📦 Пересобрать установщик
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && npx electron-builder --config electron-builder-standalone.yml --win
```
📁 **Результат:** `installer-dist-standalone\ReduxionLauncherInstaller.exe`

### 💾 Сохранить всё в Git
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && git add . && git commit -m "Описание изменений" && git push
```

### 🚀 Пересобрать лаунчер + Git push
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && npx electron-builder --config electron-builder-launcher.yml --win && git add . && git commit -m "Update launcher" && git push
```

### 📦 Пересобрать установщик + Git push
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && npx electron-builder --config electron-builder-standalone.yml --win && git add . && git commit -m "Update installer" && git push
```

### 🔄 Пересобрать ВСЁ + Git push
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && npx electron-builder --config electron-builder-launcher.yml --win && npx electron-builder --config electron-builder-standalone.yml --win && git add . && git commit -m "Build all" && git push
```

---

## 🚀 Быстрая сборка

### Только лаунчер
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher"
npx electron-builder --config electron-builder-launcher.yml --win
```

### Только установщик
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher"
npx electron-builder --config electron-builder-standalone.yml --win
```

### Всё сразу
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher"
npx electron-builder --config electron-builder-launcher.yml --win && npx electron-builder --config electron-builder-standalone.yml --win
```

---

## � Git команды

### Быстрый коммит
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher"
git add .
git commit -m "Описание изменений"
git push
```

### Одной строкой
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && git add . && git commit -m "Описание" && git push
```

### Посмотреть статус
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && git status
```

### Создать тег (новая версия)
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher"
git tag v1.0.6
git push origin v1.0.6
```

### Посмотреть все теги
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && git tag -l
```

### Удалить тег (если ошибся)
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher"
git tag -d v1.0.6
git push origin :refs/tags/v1.0.6
```

---

## 🧪 Тестирование

### Запустить лаунчер (разработка)
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher"
npx electron modern-launcher.html
```

### Запустить установщик (разработка)
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher"
npx electron installer-standalone.html
```

### Запустить собранный лаунчер
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher/launcher-dist"
./ReduxionLauncher.exe
```

### Запустить собранный установщик
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher/installer-dist-standalone"
./ReduxionLauncherInstaller.exe
```

---

## 🔄 Полный цикл выпуска новой версии

### Шаг 1: Изменить версию в файлах

**main.js** (строка 15):
```javascript
const CURRENT_VERSION = '1.0.6';
```

**modern-launcher.html** (строка ~913):
```html
<div class="version-badge">v1.0.6 (alpha)</div>
```

**installer-standalone.html** (строка ~95):
```html
<div class="version-badge">УСТАНОВЩИК v1.0.6 (alpha)</div>
```

**version.json**:
```json
{
  "version": "1.0.6",
  "installerUrl": "https://github.com/tumbwumba-dot/launcherlaunceherlalalasllasldla/releases/download/v1.0.6/ReduxionLauncher.exe"
}
```

### Шаг 2: Собрать всё
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && npx electron-builder --config electron-builder-launcher.yml --win && npx electron-builder --config electron-builder-standalone.yml --win
```

### Шаг 3: Закоммитить
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && git add . && git commit -m "Release v1.0.6" && git push
```

### Шаг 4: Создать тег
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && git tag v1.0.6 && git push origin v1.0.6
```

### Шаг 5: Загрузить на GitHub

1. Открой https://github.com/tumbwumba-dot/launcherlaunceherlalalasllasldla/releases
2. Нажми **"Draft a new release"**
3. Выбери тег **v1.0.6**
4. Название: **Reduxion Launcher v1.0.6 (alpha)**
5. Описание: список изменений
6. Прикрепи файлы:
   - `launcher-dist\ReduxionLauncher.exe`
   - `installer-dist-standalone\ReduxionLauncherInstaller.exe`
7. Нажми **"Publish release"**

---

## 🛠️ Полезные команды

### Очистить кеш сборки
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher"
rm -rf launcher-dist installer-dist-standalone node_modules/.cache
```

### Переустановить зависимости
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher"
rm -rf node_modules
npm install
```

### Посмотреть размер файлов
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher"
ls -lh launcher-dist/ReduxionLauncher.exe
ls -lh installer-dist-standalone/ReduxionLauncherInstaller.exe
```

---

## � Комбинированные команды

### Сборка лаунчера + коммит одной строкой
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && npx electron-builder --config electron-builder-launcher.yml --win && git add . && git commit -m "Update launcher" && git push
```

### Сборка установщика + коммит одной строкой
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && npx electron-builder --config electron-builder-standalone.yml --win && git add . && git commit -m "Update installer" && git push
```

### Полная сборка + коммит + тег одной строкой
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && npx electron-builder --config electron-builder-launcher.yml --win && npx electron-builder --config electron-builder-standalone.yml --win && git add . && git commit -m "Release v1.0.6" && git push && git tag v1.0.6 && git push origin v1.0.6
```

---

## ❌ Откат изменений

### Отменить последний коммит (оставить изменения)
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && git reset --soft HEAD~1
```

### Отменить последний коммит (удалить изменения)
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && git reset --hard HEAD~1
```

### Откатить файл к последнему коммиту
```bash
cd "c:/Users/neruk/OneDrive/Desktop/reduxion site/reduxion-launcher" && git checkout -- modern-launcher.html
```

---

## 📁 Пути к файлам

- **Лаунчер:** `launcher-dist\ReduxionLauncher.exe`
- **Установщик:** `installer-dist-standalone\ReduxionLauncherInstaller.exe`
- **Исходники лаунчера:** `modern-launcher.html`
- **Исходники установщика:** `installer-standalone.html`
- **Backend:** `main.js`
- **Настройки сборки лаунчера:** `electron-builder-launcher.yml`
- **Настройки сборки установщика:** `electron-builder-standalone.yml`
- **Версия для обновлений:** `version.json`

